import "package:flutter/material.dart";

class favouritesScreen extends StatelessWidget {
  const favouritesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("ABCDE"));
  }
}
